function initMap() {
    new google.maps.Map(document.getElementById("map"), {
      mapId: "557a0d92c92427a",
      center: { lat: 48.85, lng: 2.35 },
      zoom: 12,
    });
  }
  
window.initMap = initMap;